mutation updatePassword($customerId: UUID!, $password: String!, $newPassword: String!) {
  updatePassword(
    input: {
      customerId: $customerId,
      password: $password,
      newPassword: $newPassword
    }
  ) {
    boolean
  }
}